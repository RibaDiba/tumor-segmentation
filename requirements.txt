# ============================================================
# Core environment: pytorch/pytorch:2.1.2-cuda11.8-cudnn8-devel
#   Python 3.10, PyTorch 2.1.2, CUDA 11.8
#
# NOTE: torch and torchvision are provided by the base Docker /
# Apptainer image at the versions below.  When installing
# outside the container, uncomment them or install via the
# PyTorch CUDA 11.8 index:
#   pip install torch==2.1.2+cu118 torchvision==0.16.2+cu118 \
#       --extra-index-url https://download.pytorch.org/whl/cu118
# ============================================================

# --- PyTorch (pre-installed in the base image; pinned here for reference) ---
# torch==2.1.2
# torchvision==0.16.2

# --- Computer vision / image processing ---
opencv-python==4.9.0.80
pillow==10.3.0
albumentations==1.4.4        # used in src/util/preprocessing/augmentations.py

# --- Scientific computing ---
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2          # KFold splitting for cross-validation

# --- Deep-learning utilities ---
monai==1.3.0                 # DiceFocalLoss (archive scripts)

# --- HuggingFace ecosystem ---
# transformers: pinned to the tag that matches the SAM usage
# (SamModel, SamConfig, SamProcessor were added in v4.29)
transformers==4.40.2
huggingface-hub==0.23.1
datasets==2.19.1

# --- COCO tooling ---
pycocotools==2.0.7           # used throughout hooks and evaluation scripts

# --- Visualisation ---
matplotlib==3.9.0

# --- Utilities ---
tqdm==4.66.4
pyyaml==6.0.1                # imported as `yaml` in hooks and failcase scripts
wandb==0.17.0                # optional experiment tracking (lazy-imported)

# --- Jupyter / notebook support ---
ipykernel==6.29.4

# --- Dev / CI ---
pytest==8.2.1
black==24.4.2

# ============================================================
# Install-from-source packages
# These are NOT available on PyPI and must be installed
# separately (or via the git+ URLs below).
#
# segment-anything  -- Facebook Research SAM
#   Tag v1.0 (released 2023-04-05); API is stable.
#   https://github.com/facebookresearch/segment-anything/releases/tag/v1.0
#
# detectron2        -- Facebook Research Detectron2
#   Tag v0.6 is the last release built for PyTorch 2.x.
#   Pre-built wheels for torch 2.1 + CUDA 11.8 are available at:
#   https://dl.fbaipublicfiles.com/detectron2/wheels/cu118/torch2.1/index.html
#   (recommended over a source build in CI/CD to avoid NVCC dependency)
# ============================================================
git+https://github.com/facebookresearch/segment-anything.git@v1.0
# detectron2: install via pre-built wheel (see above) or:
# git+https://github.com/facebookresearch/detectron2.git@v0.6
